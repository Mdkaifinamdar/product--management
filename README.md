# Product Management

## Features

- Product Listing
- Add Product
- Edit Product
- Fetches data from `products.json file`

## Setup

1. Clone the repo
2. Run `npm install`
3. Run `npm start`
4. Run `npm install react-router-dom`
5. Run `npm run dev`